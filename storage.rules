rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /recordings/{allPaths=**} {
      // Allow anyone to upload video recordings
      allow read, write: if true;
    }
  }
}
